<% if users.empty? %>
  <p>User not found</p>
<% else %>
<% users.each do |user| %>
  <%= link_to user_path(user) do %>
    <%= attachment_image_tag user, :icon, :fill, 60, 60, fallback: "no_image.png",size:"50x50" %>
    <%= user.name %>
  <%end%>
<%end%>
<% end %>